# Plan2Go Backend

FastAPI backend for the Yatri AI frontend and the local PyTorch route recommender.

## Run

```powershell
cd d:\Desktop\Plan2Go\backend
uvicorn app.main:app --reload --port 8000
```

The API is served at `http://localhost:8000`, with OpenAPI docs at `/docs`.
